<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Night) -->
    <style name="Theme.BusTracking" parent="Theme.Material3.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_indigo</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/surface_variant</item>
        <item name="colorOnPrimaryContainer">@color/primary_indigo</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary_teal</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSecondaryContainer">@color/secondary_teal_light</item>

        <!-- Background & Surface -->
        <item name="android:colorBackground">@color/background_slate</item>
        <item name="colorSurface">@color/surface_white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primary_indigo</item>
        <item name="android:windowLightStatusBar" tools:ignore="NewApi">false</item>
    </style>

    <style name="Theme.BusTracking2" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/primary_indigo</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/secondary_teal</item>
        <item name="android:colorBackground">@color/background_slate</item>
        <item name="colorSurface">@color/surface_white</item>
        <item name="android:statusBarColor">@color/primary_indigo</item>
        <item name="android:windowLightStatusBar" tools:ignore="NewApi">false</item>
    </style>
</resources>